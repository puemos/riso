overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/**/*.ts*"
generates:
  src/generated/types.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-client"
